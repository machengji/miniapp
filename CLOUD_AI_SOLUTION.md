# Aletheia 云开发AI套餐接入方案

**文档版本**: v1.0  
**适用对象**: 微信云开发环境 + AI扩展能力  
**核心卖点**: 3行代码接入 DeepSeek/混元大模型  

---

## 📋 目录

1. [方案概述](#1-方案概述)
2. [技术架构](#2-技术架构)
3. [3行代码快速接入](#3-3行代码快速接入)
4. [完整实现详解](#4-完整实现详解)
5. [商业化方案](#5-商业化方案)
6. [成本与定价](#6-成本与定价)
7. [竞品对比](#7-竞品对比)

---

## 1. 方案概述

### 1.1 什么是云开发AI套餐？

微信云开发AI是腾讯云提供的**开箱即用**的大模型调用方案，特点：

| 特性 | 说明 |
|------|------|
| **零运维** | 无需搭建服务器，无需申请API Key |
| **低成本** | 按量付费，新用户免费额度充足 |
| **多模型** | 支持混元(hunyuan)、DeepSeek 等主流模型 |
| **双端支持** | 小程序端 + 云函数端均可调用 |
| **流式响应** | 支持 SSE 流式输出，打字机效果 |

### 1.2 为什么选择云开发AI？

**传统方案 vs 云开发AI方案**

```
传统自建服务器方案：
小程序 → 自建Node服务 → DeepSeek API → 返回
         ↑ 需要服务器、域名、备案、运维
         ↑ 需要管理API Key安全
         ↑ 需要处理流式响应

云开发AI方案：
小程序 → wx.cloud.extend.AI → 微信云端 → 返回
         ↑ 3行代码搞定
         ↑ 微信统一计费、安全保障
         ↑ 内置流式支持
```

---

## 2. 技术架构

### 2.1 整体架构图

```
┌─────────────────────────────────────────────────────────────┐
│                        小程序端                              │
│  ┌─────────────┐    ┌─────────────┐    ┌─────────────────┐ │
│  │   聊天页面   │    │  梦境日记    │    │   心灵报告页     │ │
│  │  pages/chat │    │ pages/index │    │  pages/report   │ │
│  └──────┬──────┘    └──────┬──────┘    └────────┬────────┘ │
│         │                  │                     │          │
│         └──────────────────┼─────────────────────┘          │
│                            ▼                                │
│              ┌─────────────────────────┐                    │
│              │   wx.cloud.extend.AI    │  ◄── 3行代码接入   │
│              │    客户端直接调用        │                    │
│              └───────────┬─────────────┘                    │
└──────────────────────────┼──────────────────────────────────┘
                           │
                           ▼ 安全鉴权
┌─────────────────────────────────────────────────────────────┐
│                      微信云开发后端                          │
│  ┌───────────────────────────────────────────────────────┐  │
│  │              AI 扩展能力 (wx.cloud.extend.AI)          │  │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐ │  │
│  │  │  hunyuan-exp │  │ deepseek-r1  │  │   其他模型    │ │  │
│  │  │   混元大模型  │  │ DeepSeek-R1  │  │   ...        │ │  │
│  │  └──────────────┘  └──────────────┘  └──────────────┘ │  │
│  └───────────────────────────────────────────────────────┘  │
│                            │                                │
│         ┌──────────────────┼──────────────────┐             │
│         ▼                  ▼                  ▼             │
│  ┌─────────────┐    ┌─────────────┐    ┌─────────────┐      │
│  │  云数据库    │    │   云存储    │    │  云函数     │      │
│  │   dreams    │    │   images    │    │ saveDream   │      │
│  └─────────────┘    └─────────────┘    └─────────────┘      │
└─────────────────────────────────────────────────────────────┘
```

### 2.2 支持的模型清单

| 模型ID | 名称 | 特点 | 适用场景 |
|--------|------|------|----------|
| `hunyuan-exp` | 腾讯混元 | 中文理解强，响应快 | 日常对话、摘要生成 |
| `hunyuan-turbos-latest` | 混元Turbo | 极速响应 | 实时聊天 |
| `deepseek-r1` | DeepSeek-R1 | 深度推理 | 复杂梦境分析 |
| `deepseek-v3` | DeepSeek-V3 | 通用能力强 | 多轮对话 |

---

## 3. 3行代码快速接入

### 3.1 小程序端直接调用（推荐）

```typescript
// 第1行：创建AI实例
const ai = wx.cloud.extend.AI;

// 第2行：选择模型
const model = ai.createModel('hunyuan-exp');

// 第3行：发起对话（流式）
const res = await model.streamText({ data: { messages } });
```

### 3.2 完整示例：极简对话

```typescript
// miniprogram/pages/demo/demo.ts
Page({
  data: {
    reply: ''
  },

  async sendMessage() {
    // ========== 3行核心代码开始 ==========
    const ai = wx.cloud.extend.AI;
    const model = ai.createModel('hunyuan-exp');
    const res = await model.streamText({
      data: {
        model: 'hunyuan-turbos-latest',
        messages: [
          { role: 'system', content: '你是荣格心理分析师' },
          { role: 'user', content: '我梦见自己飞起来了' }
        ]
      }
    });
    // ========== 3行核心代码结束 ==========

    // 流式接收
    for await (let chunk of res.textStream) {
      this.setData({
        reply: this.data.reply + chunk
      });
    }
  }
});
```

### 3.3 云函数端调用（适合复杂逻辑）

```javascript
// cloudfunctions/analyzeDream/index.js
const cloud = require('wx-server-sdk');
cloud.init({ env: cloud.DYNAMIC_CURRENT_ENV });

exports.main = async (event) => {
  // ========== 3行核心代码开始 ==========
  const ai = cloud.extend.AI;
  const model = ai.createModel('deepseek-r1');
  const res = await model.generateText({
    model: 'deepseek-r1',
    messages: event.messages
  });
  // ========== 3行核心代码结束 ==========
  
  return { analysis: res.text };
};
```

---

## 4. 完整实现详解

### 4.1 项目配置

#### app.json 配置

```json
{
  "cloud": true,
  "pages": [
    "pages/chat/chat",
    "pages/index/index",
    "pages/report/report"
  ],
  "window": {
    "navigationBarTextStyle": "black",
    "navigationStyle": "custom"
  },
  "requiredBackgroundModes": ["audio"],
  "lazyCodeLoading": "requiredComponents"
}
```

#### app.ts 初始化

```typescript
App<IAppOption>({
  onLaunch() {
    // 云开发初始化
    wx.cloud.init({
      env: 'your-env-id', // 替换为你的环境ID
      traceUser: true,
    });
    
    // 检查AI扩展可用性
    if (!wx.cloud.extend || !wx.cloud.extend.AI) {
      console.error('请升级基础库至 3.7.1 以上版本');
    }
  }
});
```

### 4.2 核心服务封装

```typescript
// miniprogram/services/ai.service.ts

interface AIConfig {
  model?: string;
  temperature?: number;
  maxTokens?: number;
}

interface ChatMessage {
  role: 'system' | 'user' | 'assistant';
  content: string;
}

class AIService {
  private ai = wx.cloud.extend.AI;
  
  /**
   * 流式对话（打字机效果）
   */
  async *streamChat(
    messages: ChatMessage[],
    config: AIConfig = {}
  ): AsyncGenerator<string, void, unknown> {
    const model = this.ai.createModel('hunyuan-exp');
    
    const res = await model.streamText({
      data: {
        model: config.model || 'hunyuan-turbos-latest',
        messages,
        temperature: config.temperature ?? 0.7,
        max_tokens: config.maxTokens ?? 2048
      }
    });

    for await (const chunk of res.textStream) {
      yield chunk;
    }
  }

  /**
   * 单次对话（非流式）
   */
  async chat(
    messages: ChatMessage[],
    config: AIConfig = {}
  ): Promise<string> {
    const model = this.ai.createModel('hunyuan-exp');
    
    const res = await model.generateText({
      model: config.model || 'hunyuan-turbos-latest',
      messages,
      temperature: config.temperature ?? 0.7
    });
    
    return res.text || '';
  }

  /**
   * 深度分析（DeepSeek-R1 推理模型）
   */
  async *streamReasoning(
    messages: ChatMessage[]
  ): AsyncGenerator<{type: 'thinking' | 'content', text: string}, void, unknown> {
    const model = this.ai.createModel('deepseek-r1');
    
    const res = await model.streamText({
      data: {
        model: 'deepseek-r1',
        messages
      }
    });

    let buffer = '';
    let inThinkTag = false;
    
    for await (const chunk of res.textStream) {
      buffer += chunk;
      
      // 解析 <think> 标签
      while (buffer.includes('<think>') || buffer.includes('</think>') || buffer.length > 100) {
        const thinkStart = buffer.indexOf('<think>');
        const thinkEnd = buffer.indexOf('</think>');
        
        if (thinkStart !== -1 && thinkEnd !== -1 && thinkEnd > thinkStart) {
          // 提取思考内容
          const thinking = buffer.substring(thinkStart + 7, thinkEnd);
          yield { type: 'thinking', text: thinking };
          
          // 提取正文内容（</think>之后）
          const content = buffer.substring(thinkEnd + 8);
          if (content) {
            yield { type: 'content', text: content };
          }
          
          buffer = '';
        } else if (thinkStart !== -1 && thinkEnd === -1) {
          // 思考中，还未结束
          const thinking = buffer.substring(thinkStart + 7);
          yield { type: 'thinking', text: thinking };
          buffer = '';
        } else {
          // 普通内容
          if (!buffer.includes('<think>')) {
            yield { type: 'content', text: buffer };
            buffer = '';
          }
          break;
        }
      }
    }
    
    // 处理剩余内容
    if (buffer && !buffer.includes('<think>')) {
      yield { type: 'content', text: buffer };
    }
  }
}

export const aiService = new AIService();
```

### 4.3 带记忆增强的对话实现

```typescript
// miniprogram/pages/chat/chat.ts
import { aiService } from '../../services/ai.service';

interface Message {
  id: string;
  role: 'user' | 'assistant';
  content: string;
  thought?: string;
  isThoughtExpanded?: boolean;
  isStreaming?: boolean;
}

const SYSTEM_PROMPT = `
你是一位深度的荣格流派心理分析师，代号"Aletheia"。
核心原则：
1. 严禁算命，禁止"吉凶"、"运势"等迷信词汇
2. 使用荣格概念（阴影、阿尼玛、原型）解释
3. 在<think>标签中展示推理过程
4. 语气冷峻而包容
`;

Page({
  data: {
    messages: [] as Message[],
    inputValue: '',
    isStreaming: false
  },

  async sendMessage() {
    const content = this.data.inputValue.trim();
    if (!content || this.data.isStreaming) return;

    // 1. 用户消息上屏
    this.addMessage({
      id: `msg_${Date.now()}_u`,
      role: 'user',
      content
    });

    this.setData({ inputValue: '', isStreaming: true });

    // 2. AI占位符
    const aiMsgId = `msg_${Date.now()}_a`;
    this.addMessage({
      id: aiMsgId,
      role: 'assistant',
      content: '',
      thought: '',
      isThoughtExpanded: true,
      isStreaming: true
    });

    // 3. 加载长期记忆
    const memoryContext = await this.loadMemoryContext();

    // 4. 构造消息
    const messages = [
      { role: 'system', content: SYSTEM_PROMPT + memoryContext },
      ...this.data.messages
        .filter(m => !m.isStreaming && m.content)
        .slice(-10) // 保留最近10轮
        .map(m => ({ role: m.role, content: m.content }))
    ];

    // 5. 流式接收（3行核心代码的完整版）
    try {
      for await (const chunk of aiService.streamReasoning(messages)) {
        if (chunk.type === 'thinking') {
          this.updateLastMessage({ thought: chunk.text });
        } else {
          this.updateLastMessage({ content: chunk.text });
        }
      }
    } catch (err) {
      console.error('AI调用失败:', err);
      this.updateLastMessage({ 
        content: '连接中断，请稍后再试。',
        isStreaming: false 
      });
    } finally {
      this.setData({ isStreaming: false });
      this.updateLastMessage({ isStreaming: false });
      this.saveToDatabase();
    }
  },

  async loadMemoryContext(): Promise<string> {
    try {
      const db = wx.cloud.database();
      const { data } = await db.collection('dreams')
        .orderBy('createTime', 'desc')
        .limit(5)
        .get();

      if (data.length === 0) return '';

      return `\n\n【长期记忆档案】\n${data.map((d, i) => 
        `${i + 1}. [${new Date(d.createTime).toLocaleDateString()}] ${d.summary || '无题'}`
      ).join('\n')}\n\n请分析时关联这些历史梦境。`;
    } catch {
      return '';
    }
  },

  addMessage(msg: Message) {
    this.setData({
      messages: [...this.data.messages, msg]
    });
  },

  updateLastMessage(updates: Partial<Message>) {
    const messages = [...this.data.messages];
    const last = messages[messages.length - 1];
    if (last) {
      messages[messages.length - 1] = { ...last, ...updates };
      this.setData({ messages });
    }
  },

  saveToDatabase() {
    // 保存到云数据库...
    wx.cloud.callFunction({
      name: 'saveDream',
      data: { /* ... */ }
    });
  }
});
```

### 4.4 云函数实现（非流式任务）

```javascript
// cloudfunctions/saveDream/index.js
const cloud = require('wx-server-sdk');
cloud.init({ env: cloud.DYNAMIC_CURRENT_ENV });
const db = cloud.database();

exports.main = async (event) => {
  const { content, analysis, mood, clarity } = event;
  const { OPENID } = cloud.getWXContext();

  // ===== 使用AI生成智能摘要 =====
  let summary = '无题梦境';
  try {
    const ai = cloud.extend.AI;
    const model = ai.createModel('hunyuan-exp');
    
    const res = await model.generateText({
      model: 'hunyuan-turbos-latest',
      messages: [{
        role: 'user',
        content: `为以下梦境生成一个8字以内的诗意标题，不要标点：${content}`
      }]
    });
    
    summary = res.text?.replace(/["《》]/g, '') || summary;
  } catch (err) {
    console.error('AI摘要生成失败:', err);
  }

  // 保存到数据库
  const result = await db.collection('dreams').add({
    data: {
      _openid: OPENID,
      content,
      analysis,
      summary,
      mood: mood || 'unknown',
      clarity: clarity || 3,
      createTime: db.serverDate()
    }
  });

  return { success: true, id: result._id, summary };
};
```

---

## 5. 商业化方案

### 5.1 免费额度与计费策略

#### 云开发AI免费额度（新用户）

| 资源 | 免费额度 | 说明 |
|------|----------|------|
| 混元模型 | 100万次调用 | 足够早期用户增长 |
| DeepSeek-R1 | 10万次调用 | 用于深度分析场景 |
| 云数据库 | 5GB存储 + 500万次读/月 | 足够10万用户规模 |
| 云存储 | 5GB | 图片、音频存储 |
| 云函数 | 100万次调用 | 后台任务处理 |

#### 成本结构

```
以 10,000 MAU，人均 50次对话/月 计算：

┌────────────────────────────────────────────────────┐
│  混元Turbo调用: 50万次 × ¥0.001/次 = ¥500/月        │
│  DeepSeek-R1调用: 1万次 × ¥0.005/次 = ¥50/月        │
│  云数据库: 超出免费额度 ≈ ¥20/月                    │
│  云存储: 图片存储 ≈ ¥10/月                          │
├────────────────────────────────────────────────────┤
│  月度总成本: ≈ ¥580                                │
│  单用户成本: ≈ ¥0.058/月                           │
└────────────────────────────────────────────────────┘
```

### 5.2 三级变现模式

```
┌─────────────────────────────────────────────────────────────────────┐
│                        Aletheia 变现金字塔                           │
├─────────────────────────────────────────────────────────────────────┤
│  Tier 3: 专业版 ¥68/月                                               │
│  ├── 无限次对话（混元+DeepSeek-R1）                                   │
│  ├── 解锁思考过程可视化                                              │
│  ├── 多分析师人格（弗洛伊德/阿德勒/存在主义）                          │
│  ├── 视频对话模式（AI语音交互）                                       │
│  └── 优先模型响应队列                                                │
├─────────────────────────────────────────────────────────────────────┤
│  Tier 2: 会员 ¥19/月                                                 │
│  ├── 每日30次对话（原10次）                                          │
│  ├── 解锁思考过程                                                    │
│  ├── 完整长期记忆分析                                                │
│  ├── 情绪趋势图表                                                    │
│  └── 去除广告                                                        │
├─────────────────────────────────────────────────────────────────────┤
│  Tier 1: 免费用户                                                    │
│  ├── 每日10次基础对话                                                │
│  ├── 标准回复（无思考过程）                                           │
│  ├── 7天历史记录                                                     │
│  └── 广告支持                                                        │
└─────────────────────────────────────────────────────────────────────┘
```

### 5.3 广告变现策略

| 广告位 | 触发条件 | eCPM | 预估收入 |
|--------|----------|------|----------|
| 激励视频-解锁思考 | 用户点击查看AI思考过程 | ¥80 | ¥2,400/10万DAU |
| 激励视频-生成报告 | 点击生成心灵雷达图 | ¥100 | ¥1,500/10万DAU |
| 激励视频-每日抽卡 | 共时性卡片抽取 | ¥60 | ¥1,800/10万DAU |
| 插屏广告 | 页面切换时展示 | ¥30 | ¥900/10万DAU |

**广告场景设计：**

```typescript
// 激励视频解锁思考过程
async unlockThought(messageId: string) {
  const rewardedVideoAd = wx.createRewardedVideoAd({
    adUnitId: 'adunit-xxxxx'
  });

  rewardedVideoAd.onLoad(() => {
    rewardedVideoAd.show();
  });

  rewardedVideoAd.onClose((res) => {
    if (res.isEnded) {
      // 用户完整观看，解锁思考
      this.expandThought(messageId);
      wx.showToast({ title: '已解锁深度分析' });
    }
  });
}
```

### 5.4 增值服务

| 服务 | 定价 | 说明 |
|------|------|------|
| 单次深度分析 | ¥2.99/次 | 非会员解锁DeepSeek-R1分析 |
| 月度心理报告 | ¥9.99/份 | AI生成的PDF报告 + 趋势分析 |
| 年度心灵地图 | ¥39.99/份 | 全年梦境可视化报告 |
| 梦境艺术画 | ¥4.99/张 | AI将梦境转为艺术图 |

### 5.5 B端变现（云开发AI优势）

**企业EAP心理服务：**

```
产品形态：Aletheia for Business
├── 企业版小程序（白标定制）
├── 员工匿名心理档案
├── 管理者后台（脱敏数据看板）
├── AI预警（检测高风险梦境模式）
└── 对接企业现有EAP服务商

定价：¥15/员工/年
示例：1000人企业 = ¥15,000/年
```

**优势：**
- 云开发支持快速部署企业独立环境
- 数据隔离保障隐私
- AI统一计费，企业透明结算

---

## 6. 成本与定价

### 6.1 单用户经济模型（Unit Economics）

```
假设场景：10万MAU，转化率5%

┌────────────────────────────────────────────────────────────┐
│  收入侧（月度）                                             │
├────────────────────────────────────────────────────────────┤
│  会员订阅（¥19/月）: 5,000人 × ¥19 = ¥95,000                │
│  专业版（¥68/月）: 500人 × ¥68 = ¥34,000                    │
│  广告收入: 10万DAU × ¥0.02 = ¥20,000                        │
│  增值服务: ≈ ¥5,000                                         │
├────────────────────────────────────────────────────────────┤
│  月度总收入: ¥154,000                                       │
│  年度总收入: ¥1,848,000                                     │
└────────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────────┐
│  成本侧（月度）                                             │
├────────────────────────────────────────────────────────────┤
│  AI调用成本: ¥580                                           │
│  云开发资源: ¥200                                           │
│  微信支付手续费（0.6%）: ¥924                               │
├────────────────────────────────────────────────────────────┤
│  月度总成本: ¥1,704                                         │
│  毛利率: 98.9%                                              │
└────────────────────────────────────────────────────────────┘
```

### 6.2 盈亏平衡分析

| 用户规模 | 月度成本 | 盈亏平衡订阅数 | 转化率要求 |
|----------|----------|----------------|------------|
| 1万MAU | ¥300 | 16人 | 0.16% |
| 10万MAU | ¥1,700 | 90人 | 0.09% |
| 100万MAU | ¥15,000 | 790人 | 0.08% |

**结论：** 云开发AI方案边际成本极低，规模效应明显。

---

## 7. 竞品对比

### 7.1 技术方案对比

| 维度 | 自建服务器方案 | 云开发AI方案（Aletheia） |
|------|----------------|--------------------------|
| **开发周期** | 2-4周（服务器搭建、域名备案） | 1-3天（3行代码接入） |
| **运维成本** | 高（需专人维护） | 零运维 |
| **API Key管理** | 需自行保管，有泄露风险 | 微信统一托管 |
| **流式响应** | 需自行实现SSE | 内置支持 |
| **计费透明度** | 多供应商账单 | 微信统一结算 |
| **扩容能力** | 需配置自动扩容 | 自动弹性扩容 |
| **成本** | ¥500+/月（服务器+带宽） | 按量付费，起步免费 |

### 7.2 同类心理AI产品对比

| 产品 | 定位 | 技术方案 | 变现模式 | Aletheia差异化 |
|------|------|----------|----------|----------------|
| 松果倾诉 | 真人咨询 | 传统App | 按次付费 | AI+云开发成本优势 |
| 壹心理 | 内容+咨询 | 混合架构 | 会员+课程 | 专注梦境细分领域 |
| KnowYourself | 心理测评 | 小程序 | 课程+咨询 | Chain of Thought可视化 |
| Replika | AI陪伴 | 海外API | 订阅 | 荣格理论深度分析 |
| **Aletheia** | **荣格梦境分析** | **云开发AI** | **三级订阅** | **思考过程可视化+长期记忆** |

---

## 附录

### A. 常见问题

**Q1: 云开发AI的响应速度如何？**
- 混元Turbo：平均 500ms 首字返回
- DeepSeek-R1：平均 2-3s 首字返回（推理模型）

**Q2: 是否支持多轮对话上下文？**
- 支持，需在客户端自行维护消息历史，随请求发送

**Q3: 数据隐私如何保障？**
- 微信云开发符合国内数据合规要求
- 数据存储在腾讯云，不出境
- 支持数据库权限控制

**Q4: 免费额度用完怎么办？**
- 可购买云开发资源包
- 或切换到按量付费（后付费）

### B. 参考资源

- [微信云开发文档](https://developers.weixin.qq.com/miniprogram/dev/wxcloud/basis/getting-started.html)
- [云开发AI扩展文档](https://developers.weixin.qq.com/miniprogram/dev/wxcloudrun/src/basic/ai.html)
- [DeepSeek API文档](https://platform.deepseek.com/api-docs)

---

**文档维护**: Aletheia Team  
**最后更新**: 2026-01-31
